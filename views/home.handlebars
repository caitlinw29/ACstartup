{{#each bugs as |bug| }}
  <div class="card text-center">
    <div class="card-header">
      <img class="creature-img" src="{{bug.icon}}" alt="{{bug.name}}"/>
      <h2> {{bug.name}} </h2>   
    </div>
  
    <div class="card-contents">
      <p> Sell price: {{bug.sellPriceString}} bells </p>
      <p> Flick price: {{bug.flickPriceString}} bells </p>
      <p> Available: {{bug.monthN}} </p>
      <p> Months: {{bug.monthArrayN}}</p>
      <p> Time: {{bug.time}} </p>
      <p> Location: {{bug.location}}</p>
      {{greaterThanZeroBug bug.total_catch}}
      <button class="bugDonateBtn donateButton" id="{{bug.id}}" data-name="{{bug.name}}">Donate</button>
    </div>
  </div>
{{/each}}

{{#each fish as |fish| }}
  <div class="card text-center">
    <div class="card-header">
      <img class="creature-img" src="{{fish.icon}}" alt="{{fish.name}}"/>
      <h2> {{fish.name}} </h2>
    </div>

    <div class="card-contents">
      <p> Sell price: {{fish.sellPriceString}} bells </p>
      <p> CJ price: {{fish.cjPriceString}} bells </p>
      <p> Available: {{fish.monthN}} </p>
      <p> Months: {{fish.monthArrayN}}</p>
      <p> Time: {{fish.time}} </p>
      {{Char-ChSalmon fish.name}}
      {{greaterThanZeroFish fish.total_catch}}
      <p> Location: {{fish.location}}</p>
      <p> Shadow: {{fish.shadow}} </p>
      <button class="fishDonateBtn donateButton" id="{{fish.id}}" data-name="{{fish.name}}">Donate</button>
    </div>
  </div>
{{/each}}

{{#each sea as |sea| }}
  <div class="card text-center">
    <div class="card-header">
      <img class="creature-img" src="{{sea.icon}}" alt="{{sea.name}}"/>
      <h2> {{sea.name}} </h2>
    </div>

    <div class="card-contents">
      <p> Sell price: {{sea.sellPriceString}} bells </p>
      <p> Available: {{sea.monthN}} </p>
      <p> Months: {{sea.monthArrayN}}</p>
      <p> Time: {{sea.time}} </p>
      <p> Speed: {{sea.speed}}</p>
      <p> Shadow: {{sea.shadow}} </p>
      {{greaterThanZeroSea sea.total_catch}}
      <button class="seaDonateBtn donateButton" id="{{sea.id}}" data-name="{{sea.name}}">Donate</button>
    </div>
  </div>
{{/each}}

<script src="./js/bug.js"></script>
<script src="./js/fish.js"></script>
<script src="./js/sea.js"></script>