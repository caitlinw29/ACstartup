<h1>Home</h1>
{{!-- {{#each bugs as |bug| }}
  <div class="card">
    <div class="card-header">
      <h2>
        {{bug.name}} 
      </h2>
      <p>
        Sell price: {{bug.sellPrice}} bells
      </p>
    </div>
    <p class="card-contents"><button class="bugDonateBtn" id="{{bug.id}}">Donate</button></p>
  </div>
{{/each}} --}}

{{#each fish as |fish| }}
  <div class="card">
    <div class="card-header">
      <h2>
        {{fish.name}} 
      </h2>
      <p>
        Sell price: {{fish.sellPrice}} bells
      </p>
    </div>
    <p class="card-contents"><button class="fishDonateBtn" id="{{fish.id}}">Donate</button></p>
  </div>
{{/each}}

<script src="./js/bug.js"></script>
<script src="./js/fish.js"></script>